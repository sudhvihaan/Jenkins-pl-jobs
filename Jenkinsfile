pipeline {
    agent {
        label 'jenkins-slave'  // Corrected the typo here
    }
    stages {
        stage('Build Job1') {
            steps {
                echo 'Running Job1...'
            }
        }
    }
}

